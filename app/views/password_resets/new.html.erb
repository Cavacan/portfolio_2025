<h1>パスワードリセット</h1>
<div class="container">
  <div class="mb-3">
    <p>登録メールアドレス宛にパスワードリセット用のURLを送信します。</p>
    <p>以下のフォームにメールアドレスを入力して「送信」ボタンを押して下さい。</p>
  </div>
  <br>
  <%= form_with url: password_resets_path, method: :post do |f| %>
    <div class="d-flex">
      <div class="m-3">
        <%= label_tag :email, '登録メールアドレス：' %>
        <%= email_field_tag :email, "", size: "30" %>
      </div>
      <div class="m-3">
        <%= submit_tag "送信", style: 'width: 150px;', class: 'btn btn-success' %>
      </div>
    </div>
    <div class="mt-5"></div>
    <div class="mt-3">
      <%= link_to 'ログイン画面に戻る', root_path, class: 'btn btn-danger' %>
    </div>
  <% end %>
</div>
